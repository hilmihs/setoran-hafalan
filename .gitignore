node_modules/
.next/
out/
build/
.env
.env.local
.env*.local
*.log
.DS_Store
.vercel
next-env.d.ts
tsconfig.tsbuildinfo

# pakai npm sebagai package manager utama
pnpm-lock.yaml
pnpm-workspace.yaml

# data files
*.xlsx
*.pdf

# Playwright MCP transient artifacts
.playwright-mcp/

# Supabase full backup (data JSON + audio + dumps). Berisi data sensitif
# (hash password, nomor WA). JANGAN commit.
_backup_supabase/
# Dump SQL berisi SEMUA data (hash password, nomor WA, data pribadi). JANGAN
# commit — dikirim ke penerima bersama _backup_supabase, atau regen via
# `npm run generate-dump`.
db-migration/maahir_full_dump.sql

# Arsip handoff DB (data sensitif). JANGAN commit.
maahir-db-handoff.tar.gz
