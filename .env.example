# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...

# Service role key — HANYA dipakai di server (API routes, scripts). JANGAN expose ke client.
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Iron-session cookie secret — generate dengan: openssl rand -base64 32
SESSION_SECRET=ganti-dengan-32-karakter-acak-yang-panjang-sekali

# Nama bucket Supabase storage untuk audio
SUPABASE_AUDIO_BUCKET=setoran-audio

# Untuk wa.me link generation (opsional, kalau mau pakai short URL nanti)
NEXT_PUBLIC_APP_URL=http://localhost:3000
